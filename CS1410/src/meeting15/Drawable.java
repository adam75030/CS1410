package meeting15;

public interface Drawable {
	// just like abstract classes
	// we can define this interface
	// have other classes IMPLEMENT it
	// then we can have arrays, or dynamic arrays, or methods that use Drawable
	// types

	// similar to a VERY abstract class
	// no instance variables
	// only abstract methods

	/**
	 * This is a method that draws a pictoral representation of the thing to the
	 * command line
	 */
	void draw();

}
